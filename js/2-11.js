function calculateEngravingPrice(message, pricePerWord) {
  // Пиши код ниже этой строки
  const string = message.split(' ');

  return string.length * pricePerWord;

  // Пиши код выше этой строки
}
